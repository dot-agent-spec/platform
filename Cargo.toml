[workspace]
members = [
    "packages/tree-sitter",
    "packages/parser-dsl",
    "packages/kernel-dsl",
    "tools/wasi-stub",
]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "s"
