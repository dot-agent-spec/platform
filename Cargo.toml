[workspace]
members = [
    "packages/tree-sitter",
    "packages/parser-dsl",
    "packages/kernel-dsl",
]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "s"
